#include <stdio.h>

/**
 * main - prints with "printf",
 * Principal function,
 * Return: 0 at the end
 */ 
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

