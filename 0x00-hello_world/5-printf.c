#include <stdio.h>

/**
 * main - prints the phrase with proper grammar,
 * simple principal fuction,,
 * Return: 0 at the end
 */
int main(void)
{
		printf("with proper grammar, but the outcome is a piece of art,\n");
			return (0);
}
