#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @x: first int 
 * @y: second int 
 * Return: the sum 
 */
int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
