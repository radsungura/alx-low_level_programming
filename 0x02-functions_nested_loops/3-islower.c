#include "main.h"

/**
 * _islower - checks for lowercase character.
 * @c: is the character to check,
 * Return: 1 if 'c' is lowercase, 0 ortherwise
 */
int _islower(int c)
{
		return (c >= 'a' && c <= 'z');
}
